#pragma once

#include <godot_cpp/classes/resource.hpp>

struct ColorPalette : godot::Resource
{
    GDCLASS(ColorPalette, godot::Resource)

private:

private:
    static void _bind_methods()
    {

    }
}; // ColorPalette